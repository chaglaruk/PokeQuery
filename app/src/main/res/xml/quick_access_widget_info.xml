<?xml version="1.0" encoding="utf-8"?>
<!--
  v0.6.1 — Quick Access home screen widget.

  Tap opens the app to the Safe Cleanup review screen (the safest, most-used entry). The widget
  does NOT silently copy to the clipboard — it opens the app, where Copy + RiskWarning apply as
  usual. No new permission, no network, no background updates.

  First widget iteration is non-configurable (Option B). A future configurable version could let
  the user pick a target (Safe Cleanup / Candy Prep / Trade Fodder / Expert / a Personal Preset).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="40dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_quick_access"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_quick_access"
    android:description="@string/widget_quick_access_description" />
