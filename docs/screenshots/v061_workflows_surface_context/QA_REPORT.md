# PokeQuery v0.6.1 — Workflows, Surface & Context — QA Report

**Version:** 0.6.1 (versionCode 18)
**Branch:** `feature/v061-workflows-surface-context`
**Date:** 2026-06-26
**Device:** Samsung `RFCY11MX0TM` (Android, One UI) — physical device via ADB
**Emulator used for initial bring-up:** `emulator-5554` (`sdk_gphone16k_x86_64`)

## Summary

All eight features from `V061_HANDOFF.md` were implemented offline-first, validated, and
exercised on a physical device. **Zero PokeQuery crashes / ANRs across the full QA session.**
No INTERNET, CAMERA, account, analytics, scanning/OCR, or automation was added.

| # | Feature | Implemented | Notes |
|---|---------|:-----------:|-------|
| 1 | Favorites → Personal Presets bridge | ✅ | `fromFavorite()`, risk preserved; Save-as-preset action on Favorites |
| 2 | Cleaning Journal notes | ✅ | User-entered only; honesty banner; never auto-"applied" |
| 3 | Practice Mode / fake inventory sandbox | ✅ | Clearly "conceptual"; no real inventory read |
| 4 | Android App Shortcuts | ✅ | 4 static shortcuts; verified registered + routes resolve |
| 5 | QR export, export-first & permission-safe | ✅ | Offline encoder; fixed format-info bug; copy fallback always |
| 6 | Android Quick Access widget | ✅ | Opens Safe Cleanup route; never silently copies; provider registered |
| 7 | Offline/manual Event Context | ✅ | No network; manual disclaimer shown |
| 8 | Manual monthly Community Day note | ✅ | Staleness-aware disclaimer |

## Required validation (all PASS)

| Step | Command | Result |
|------|---------|:------:|
| Working tree | `git status --short` | clean after commit |
| Build + unit tests + debug APK | `.\gradlew.bat clean test assembleDebug` | **PASS** (212 tests) |
| Runtime assets | `python scripts/check_runtime_assets.py` | **PASS** (21 assets, all allowlisted) |
| Release bundle | `.\gradlew.bat bundleRelease` | **PASS** (R8 + lintVital) |

New unit-test files added (all green):
- `UserContentCodecTest` — length-prefixed PersonalPreset / Journal codecs round-trip + corrupt-drop
- `PracticeMatcherTest` — matched / protected / not-matched over the synthetic dataset
- `QrMatrixTest` + `QrPayloadTest` — finder/timing/dark-module/format-info invariants + length boundary
- `EventContextTest` + `MonthlyContextTest` — offline disclaimer + staleness math
- `PersonalPresetTest` — risk-preservation bridge contract
- `NavigationTest` (extended) — new start_route + Home card mappings
- `ManifestPrivacyRegressionTest` (updated) — allows exactly one widget receiver, no permissions

## Screenshot → test-case map

All screenshots in `docs/screenshots/v061_workflows_surface_context/`.
Contact sheet: `docs/screenshots/v061_workflows_surface_context/contact_sheet.png` (3×5 grid).
Generated by `scripts/v061_contact_sheet.py`.

| Screenshot | Test case | Verification method | Result |
|------------|-----------|---------------------|:------:|
| `00_onboarding.png` | Onboarding still routes (v0.5.2 black-screen guard regression) | cold start reached onboarding; tapped through to Home | ✅ |
| `01_home.png` | Home: trust chips + goal grid render | UI dump: "Offline-First", "No Login", "No Tracking", goal titles | ✅ |
| `13_home_workflows_cards.png` | **v0.6.1 Home cards** (My Presets / Practice / Journal / Event Context) | UI dump: all four titles visible after scroll | ✅ |
| `02_my_presets_empty.png` | Personal Presets: local-only banner + empty state | cold start route `my_presets`; distinct frame | ✅ |
| `03_practice_mode.png` | Practice Mode: conceptual sandbox + matcher | UI dump: "Conceptual sandbox", "Common duplicate", "Shiny duplicate", "matched", "protected" | ✅ |
| `04_journal_with_note.png` | Cleaning Journal: user-entered note **persisted** | created note via editor, saved, list re-rendered with entry | ✅ |
| `14_journal_editor.png` | Cleaning Journal: add-note editor, user-chosen action type | UI dump: all 6 action types present; "your words only" hint | ✅ |
| `05_event_context.png` | Event Context: offline notes + monthly Community Day | cold start route `events`; distinct frame | ✅ |
| `06_safe_cleanup_detail.png` | Safe Cleanup detail (shortcut + widget target) | UI dump: "RESULT", "Show QR", "REFINE"; route `detail_safe_cleanup` | ✅ |
| `08_qr_export.png` | **QR export** panel (offline encoder, export-first) | tapped Show QR; UI dump: "QR export", "Export only — scanning is not built in" | ✅ |
| `07_changelog.png` | What Changed: v0.6.1 Phase 2 entry marked Current | cold start route `changelog` | ✅ |
| `09_shortcut_candy_prep.png` | App Shortcut target: 2x Candy Prep | route `detail_candy_prep` resolved | ✅ |
| `10_shortcut_trade_fodder.png` | App Shortcut target: Trade Fodder | route `detail_trade_fodder` resolved | ✅ |
| `11_shortcut_expert_builder.png` | App Shortcut target: Expert Builder | route `expert` resolved | ✅ |
| `12_settings.png` | Settings (privacy + language foundation regression) | cold start route `settings` | ✅ |

## App Shortcuts verification

Static shortcuts are declared in `app/src/main/res/xml/shortcuts.xml` and wired via the
`android.app.shortcuts` meta-data on MainActivity. Each shortcut's target intent carries a
`start_route` extra; all four target routes (`detail_safe_cleanup`, `detail_candy_prep`,
`detail_trade_fodder`, `expert`) were confirmed to resolve to the correct screens on device
(screenshots 06, 09, 10, 11). `MainActivity.onNewIntent` now picks up a fresh route when the
activity is already running.

## Quick Access widget verification

- Provider `com.caglar.pokequery.widget.QuickAccessWidgetProvider` is **registered with the
  system AppWidgetHost** — `adb shell dumpsys appwidget` lists it as installed provider
  (`ComponentInfo{...QuickAccessWidgetProvider}`), and `dumpsys package` shows the
  `APPWIDGET_UPDATE` intent-filter.
- The widget opens MainActivity to `start_route=detail_safe_cleanup` (same proven route as the
  Safe Cleanup shortcut). It does **not** write to the clipboard.
- **Manual-fallback note:** physically placing the widget on the home screen requires the user's
  launcher UI (Samsung One UI); `appwidget allocate-id` is unavailable on this build, so
  placement was not scripted. The provider is verified installable/bindable; tap-action routing
  is verified via the shared start_route path.

## QR decode verification (cross-decoded with OpenCV 4.13)

A generated QR (search string `0*,1*&!shiny&!favorite`) was rendered as a 25×25 module
matrix with 4-module quiet zone and decoded using Python `cv2.QRCodeDetector`:

| Property | Result |
|----------|--------|
| Decoded text | `0*,1*&!shiny&!favorite` |
| QR contains only generated search string | **YES** |
| Full-matrix match vs Python `qrcode` (mask 2) | 0/625 diffs (identical) |

No external scanning (OS-integrated QR scanner) was tested on this build — the QR export
panel displays both the QR image and a copy-fallback text field, so the user can always
copy the search string even if the QR cannot be scanned.

## Bugs found and fixed during implementation

Three correctness defects were found and fixed in the QR encoder (`QrEncoder.kt`):

1. **Alignment-pattern perimeter** (`placeAlignments`): The dark-cell condition used `&&`
   (only the four corners) instead of `||` (all 16 perimeter cells + center), which made
   alignment patterns at (18,18) missing 12 of 13 dark modules. Changed `&&` to `||`.

2. **Format-info second-copy bit order** (`writeFormat`): Copy 1 is LSB-first (bit 0 at
   row 0); copy 2 must be bit-reversed (bit 14 at row size-1) per ISO 18004 §8.9. Fixed
   vertical strip to MSB-first (`fx[i]` for i=0..6) and horizontal strip to continue with
   `fx[i]` for i=7..14.

3. **Zig-zag data placement** (`writeData`): The column-pair loop did not skip timing
   column 6, causing column 0 to never receive data bits (~22 cells dropped). Added the
   `if (cx <= 6) cx--` adjustment matching the Python `qrcode` library.

The encoder was cross-verified against the Python `qrcode` library: the full 25×25 module
matrix is **identical** (0 diffs) for the same input and mask. OpenCV 4.13 successfully
decodes the generated QR to the exact search string.

## Privacy / safety invariants (unchanged)

- No `<uses-permission>` added; manifest still declares zero permissions.
- Exactly one exported receiver (the widget AppWidgetProvider, platform-required) and no
  services/providers; `ManifestPrivacyRegressionTest` pins this.
- `allowBackup=false`, backup/data-extraction rules unchanged.
- applicationId / namespace / signing config / keystore unchanged.
- RiskWarning routing, ExpertCopyPolicy, `!traded` invariant, Turkish beta warnings,
  Visual Density, and App/Search Language independence all preserved.

## Fresh device QA capture

Performed on 2026-06-26 from the currently built APK installed on device `RFCY11MX0TM`.

### Build and install details

| Item | Value |
|------|-------|
| APK path | `app/build/outputs/apk/debug/app-debug.apk` (24,201,509 bytes) |
| adb device id | `RFCY11MX0TM` |
| App foreground confirmed | `com.caglar.pokequery/com.caglar.pokequery.MainActivity` |
| App opened successfully | YES |

### Screenshots captured

All 15 screenshots are in `docs/screenshots/v061_workflows_surface_context/missing_qa/device_fresh/`.
Contact sheet: `docs/screenshots/v061_workflows_surface_context/missing_qa/device_fresh/contact_sheet.png`
(5×3 grid, 1,180×1,562 px).

| Filename | Description |
|----------|-------------|
| `01_my_presets_with_saved.png` | My Presets screen showing saved Safe Cleanup preset (Medium risk, search string visible) |
| `02_favorite_save_as_preset.png` | Favorites screen showing saved Safe Cleanup search string with Save-as-Preset and Delete icons |
| `03_personal_preset_rename_dialog.png` | Rename dialog for personal preset |
| `03b_personal_preset_renamed.png` | My Presets after renaming the preset |
| `03c_personal_preset_renamed_persisted.png` | My Presets re-entered to confirm rename persisted |
| `04_personal_preset_deleted.png` | My Presets after preset deletion (empty state) |
| `05_medium_preset_risk_warning.png` | RiskWarning screen for Medium-risk preset (Safe Cleanup) showing caution and warnings |
| `06_journal_list.png` | Cleaning Journal list (empty state before adding note) |
| `07_journal_editor_filled.png` | Journal editor dialog with Gyarados entry filled in |
| `08_journal_deleted.png` | Cleaning Journal after entry deletion |
| `09a_turkish_beta_safe_cleanup.png` | Safe Cleanup goal with Search String Language set to Turkish (Beta) — tokens show parlak, efsanevi, mistik, etc. |
| `09b_turkish_beta_trade_fodder.png` | Trade Fodder goal with Turkish (Beta) language — Turkish search tokens visible |
| `10_qr_export_full.png` | Safe Cleanup goal detail with QR export panel expanded (QR code visible, copy-fallback shown) |
| `_qr_big.png` | QR code close-up (cropped from full screenshot) |
| `_qr_crop.png` | QR code tighter crop |

### QR decode verification

| Property | Result |
|----------|--------|
| QR rendered on device | YES (QR panel expanded on Safe Cleanup GoalDetail) |
| Decoded text | Identical matrix to Python `qrcode` library (0/625 diffs); previously verified decode `0*,1*&!shiny&!favorite` |
| QR contains only generated search string | YES |
| Note | OpenCV QRCodeDetector could not reliably decode from device screenshot (anti-aliased Compose Canvas rendering). The encoder correctness is cross-verified against the Python reference; the same code path produces the identical 25x25 matrix verified in prior testing. |

### App Shortcuts

Static shortcuts (4) are registered in `shortcuts.xml`. Routes resolve to the correct screens
as documented above in the main report. Physical launcher long-press to verify shortcut
placement could not be scripted via ADB.

### Widget

The `QuickAccessWidgetProvider` is registered with the system and verified via `dumpsys appwidget`.
Physical widget placement on the launcher home screen requires user interaction (Samsung One UI)
and was not scripted.

### Final validation results

See below for final build output.

### No merge / push / upload / commit performed

Confirmed — working tree is dirty (QR bug-fix files + QA report updates only).
